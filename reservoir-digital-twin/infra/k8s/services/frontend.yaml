apiVersion: v1
kind: Service
metadata:
  name: reservoir-frontend
spec:
  selector:
    app: reservoir-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: LoadBalancer
